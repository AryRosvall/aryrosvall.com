@import "../vars.styl"

body
  background-color body-color
  color Header-color
  font-family 'Rubik', sans-serif
  margin: 0;
  padding: 0;
  height: 100%;
  overflow-x: hidden;

h1, h2 {
  padding 0
  margin 0
}

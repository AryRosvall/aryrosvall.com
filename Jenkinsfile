pipeline {
  agent any
  
  stages {
    
    stage("build") {
      steps{
        echo 'building the app'
        echo 'another step'
      }
    }
    
     stage("test") {
      steps{
        echo 'testing the app'
      }
    }
    
     stage("deploy") {
      steps{
        echo 'deploy the app'
      }
    }
  }
}
